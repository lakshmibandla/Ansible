- name: create users
  hosts: localhost
  become: yes
  tasks: 
  - name: checking user
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
      git
      mysql
      
      